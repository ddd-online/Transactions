# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

个人用户，日常管理自己的收支记录。使用场景是桌面端（Windows），在自己的电脑前安静地记账、回顾消费、分析趋势。用户注重隐私——所有数据存在本地 SQLite，不上传任何云端。

## Product Purpose

一款桌面端个人记账工具，帮助用户追踪多账本的交易记录、分类管理收支、通过图表分析消费习惯、将交易关联到生活关键事件、用日记沉淀日常，并可用 AI 助手直接提问账目情况。核心价值是数据完全本地化——不需要账号、不联网、绝对隐私；AI、日记等增强能力都围绕本地账本数据展开，不改变本地优先的承诺。

## Positioning

数据完全本地化的桌面记账工具。不像网页版记账工具依赖云服务，也不像企业财务软件那样复杂沉重——它是个人用户自己电脑上的私人账房，既专业又安静。

## Operating Context

（以下为仓库证据推断的事实）Windows 桌面应用，安装后以独立窗口运行。首次启动选择或创建工作空间文件夹，之后数据只保存在本地：SQLite 数据库存放账目记录，关键事件图片附件等也留在本地。应用离线可用、无需账号、单实例运行。日常使用节奏：在电脑前安静地录入与回顾交易、看趋势图表、在日历上标记生活事件、按日期写日记，偶尔用 AI 助手查询账目。关闭行为（退出或最小化到托盘）可由用户配置。

## Capabilities and Constraints

以下能力事实均基于仓库代码与文档确认：

- **多账本**：一个工作空间内可创建多个独立账本，每个账本拥有独立的分类/标签体系和交易记录
- **交易管理**：收入/支出/转账，分类、标签、模板、离群值标记、描述；多条件组合筛选与最多 4 级排序；金额以整数分存储，显示一律使用等宽数字
- **数据分析**：预设与自定义图表（折线/柱状/饼图），按年/月粒度，与账本和时间范围联动
- **关键事件**：全年日历 + 事件详情（标题、20 种颜色标记、最长 5000 字正文、多图附件，支持粘贴与 HEIC 图片），可将交易关联到事件日期并汇总关联金额
- **日记**：按日期记录日记（含心情），日期树导航、跳转日期、保存与删除
- **AI 助手**：可配置模型提供商（默认 DeepSeek），多角色（财务助手、日记助手），流式回答、思考过程展示、工具调用、快捷指令；对话上下文基于当前账本
- **设置**：分类与标签、消费模板、工作空间切换、AI 配置、日记设置、通用设置、关于
- **更新**：应用内检查/下载/安装更新，发布渠道为 GitHub Release（当前版本 0.15.1）

约束：

- 所有数据存于本地工作空间；无账号体系、无云端同步（仓库中有 sync 相关规划文档，但未发现已实现的功能）
- 仅浅色模式（设计系统 DESIGN.md 明确不支持深色）
- 工程栈：Electron + Vue 3 + Go/Gin，SQLite 纯 Go 驱动（无需 CGO）
- 首次启动双窗口流程：先选择工作空间，再进入主界面；无系统原生窗口边框（frameless）

## Brand Commitments

产品名 **Transactions**。品牌人格：**calm, restrained, precise**（宁静、克制、精确），这是已确认的身份承诺。视觉身份约束由 DESIGN.md 确立：仅浅色模式、单一主色 muted-teal（#4A8E70）、语义色（收入/支出/转账）只出现在交易数据处。工程标识使用 `--billadm-` 前缀（内部命名，非用户可见品牌）。

## Evidence on Hand

- `docs/frontend-features.md`：功能说明（部分内容已过时，其中"明暗主题"与当前仅浅色模式的实现不符）
- `DESIGN.md`：设计系统文档
- 未发现任何用户证言、媒体报道、定价或部署证据；不得凭空编造这些内容

## Product Principles

1. **宁静胜于喧闹** — 界面应该像一间安静的书房，不是嘈杂的交易大厅。每个元素都要有存在的理由。
2. **数据私有，界面专业** — 隐私是本地的默认，专业是呈现的标准。不因为离线就降低体验。
3. **数字是主角** — 金额是最重要的信息。等宽数字、清晰的层级、恰当的留白让数字自己说话。
4. **桌面原生感受** — 这不是一个套在 Electron 壳里的网页，而是一个真正的桌面应用。窗口控制、拖拽区域、原生交互。
5. **少即是多** — 不堆砌功能。默认展示最重要的信息，次要信息按需展开。

## Brand Personality

宁静、克制、专注。像一个安静的私人书房——光线温暖、没有干扰、数字清晰。不是喧闹的交易大厅，不是冰冷的银行柜台，也不是游戏化的打卡应用。

三个词：**calm, restrained, precise**（宁静、克制、精确）。

## Anti-references

- 花哨过载的 SaaS dashboard（过多装饰、彩色卡片、浮夸动画）
- 冰冷的企业财务软件（密集表格、灰色单调、缺乏温度）
- 可爱游戏化的记账应用（卡通插画、弹跳动画、徽章系统）
- AI 生成的 SaaS 奶油色模板（cream/sand/beige 暖中性色作为默认背景的套路）

## Accessibility & Inclusion

无特殊合规要求。保持基本可用性：足够的色彩对比度、色盲友好的收入/支出/转账配色（已通过不同色相区分）、键盘可导航；代码中已实现 `prefers-reduced-motion` 降级。
